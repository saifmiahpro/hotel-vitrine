# Environment Variables Example
# Copy this file to .env and fill in your values if needed

# Site URL (update for production)
# PUBLIC_SITE_URL=https://your-hotel-domain.com

# Google Analytics (optional)
# PUBLIC_GA_TRACKING_ID=G-XXXXXXXXXX

# Form Service (if using external service instead of Netlify Forms)
# PUBLIC_FORMSPREE_ID=your-form-id
# PUBLIC_BASIN_ID=your-basin-id

# Note: Variables prefixed with PUBLIC_ are exposed to the client
# For sensitive keys, use server-side only (no PUBLIC_ prefix)
